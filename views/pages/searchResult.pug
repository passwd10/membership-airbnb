extends ../layout

block content
  h1= "검색결과"
  ul
    each room in rooms
      li
        h3= room.title
        div= room.description1
        div= room.description2
        div= room.price
        img(src=room.image)

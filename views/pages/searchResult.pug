extends ../layout

block content
  h1= `${place}의 숙소`
  p= `${rooms.length}개`
  p= `${personnel}명`
  ul
    each room in rooms
      li
        h3= room.title
        div= room.description1
        div= room.description2
        div= room.price
        img(src=room.image)
